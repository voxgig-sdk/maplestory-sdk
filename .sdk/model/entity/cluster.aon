# Entity: cluster

main: kit: entity: cluster: {

  alias: field: {}

  fields: [
    {
      name: "hostname"
      req: false
      type: "`$STRING`"
    }
    {
      format: "date-time"
      name: "lastSeen"
      req: false
      type: "`$STRING`"
    }
    {
      name: "metrics"
      req: false
      type: "`$OBJECT`"
    }
  ]

  name: "cluster"
  op: {
    list: {
      name: "list"
      points: [
        {
          method: "GET"
          orig: "/api/metrics/cluster"
          segments: [
            {
              lit: "api"
            }
            {
              lit: "metrics"
            }
            {
              lit: "cluster"
            }
          ]
          transform: {
            req: "`reqdata`"
            res: "`body.cluster`"
          }
        }
      ]
    }
  }



}
